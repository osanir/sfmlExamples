#include "include/Game.h"
#include "include/Menu.h"
//#include "include/MapEditor.h"

int main(){
/*
    Menu menu(640, 480);
    int scene = menu.run();
    if( scene ==  0){   // 0 : play
        Game game;
        game.run();        
    } 
*/
    Game game;
    game.run();
    return 0;
}